# Checkpoint 1

Use `scripts/checkpoint1/mbgm_audit.py` for the real Mercedes-Benz data audit.
Use `scripts/checkpoint1/run_upstream_audit.sh` inside the upstream `dc_tabeval` repository for real environment/run evidence.

Do not submit placeholder outputs. Verify the actual Git commit and line numbers locally.
